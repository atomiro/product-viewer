{
  "name": "viewer-release",
  "version": "1.0.0",
  "description": "tasks for concat + uglifying",
  "main": "Gruntfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Arielle Soutar",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-concat": "^1.0.0",
    "grunt-contrib-watch": "^0.6.1",
    "grunt-contrib-uglify": "^2.0.0"
  }
}
